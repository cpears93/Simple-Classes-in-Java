import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class CalculatorTest {

    Calculator calculator

    @Test
    public void hasAdd(){
    assertEquals("Add", calculator.getAdd);
    }
}
